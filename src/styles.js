import { makeStyles } from '@material-ui/core';

export default makeStyles({
  chatBotContainer: {
    position: 'absolute',
    right: '25px',
    bottom: '25px',
    width: '23vw',
    textAlign: 'center',
  },
  chatBot: {
    display: 'flex',
    justifyContent: 'center',
  },
});
