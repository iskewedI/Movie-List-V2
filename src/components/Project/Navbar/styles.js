import { makeStyles } from "@material-ui/core";

export default makeStyles({
  nav: {
    height: "50%",
  },
  toSeeCounter: {
    position: "absolute",
    marginLeft: "5px",
    fontSize: "16px",
  },
});
