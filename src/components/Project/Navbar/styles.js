import { makeStyles } from "@material-ui/core";

export default makeStyles({
  nav: {
    height: "50%",
  },
  toSeeCounter: {
    position: "absolute",
    marginLeft: "5px",
    fontSize: "16px",
  },
  newListForm: {
    position: 'absolute',
    top: "50px",
    left: "15px"
  }
});
