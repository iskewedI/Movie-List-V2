import { makeStyles } from "@material-ui/core";

export default makeStyles({
  accordionsContainer: {
    backgroundColor: "#222",
    display: "grid",
    gridTemplateRows: "repeat(2, 1fr)",
  },
  accordionRow: {},
});
